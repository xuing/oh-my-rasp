apiVersion: v1
kind: Service
metadata:
  name: ohmyrasp-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: ohmyrasp-api
  ports:
    - name: http
      port: {{ .Values.service.apiPort }}
      targetPort: 8080
